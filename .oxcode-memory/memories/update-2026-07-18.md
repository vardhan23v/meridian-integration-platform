---
title: Update 2026-07-18
slug: update-2026-07-18
tags: 
scope: project
updated_at: 2026-07-18T22:27:53.260Z
source: live
hook: Created `IntegrationJobsTable.tsx` and `DLQTable.tsx` frontend table components
---

- Created `IntegrationJobsTable.tsx` and `DLQTable.tsx` frontend table components
- Implemented glass-morphism UI style consistent with existing dashboard components
- Used shadcn/ui-inspired table structure with proper column definitions
- Integrated loading, error, and empty state handling for both tables
- Added action buttons (Trigger, Retry, Resolve) wired to respective hooks
- Utilized `use client` directive and lucide-react icons
- Applied consistent styling with `glass` background and `border-white/10`
- Followed existing design patterns for hover and transition effects
- Components compile without TypeScript errors
- Tables display data from `useIntegrationJobs` and `useDLQ` hooks respectively
- Action buttons properly disable during in-flight mutations
- Status badges use color-coding for visual indication
- Error states show appropriate messages and icons
- Empty states handled gracefully with placeholder content
• Replaced placeholder markup in `apps/web/src/app/(dashboard)/dashboard/jobs/page.tsx` with `<IntegrationJobsTable />` component
• Replaced placeholder markup in `apps/web/src/app/(dashboard)/dashboard/dlq/page.tsx` with `<DLQTable />` component
• Both new table components rendered with consistent header styling using `var(--foreground)` color and `tracking-tight`
• Removed old placeholder white-card markup from both pages
• Verified no build or runtime diagnostics issues after changes
• Maintained interface contract: `JobsPage` and `DLQPage` export default JSX elements as required
• CI workflow updated to trigger on both `main` and `master` branches for push and pull request events
• No other changes made to workflow configuration beyond adding `master` to branch lists
• Workflow remains unchanged otherwise, maintaining existing job definitions and steps
